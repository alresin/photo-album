{
  "name": "photo-album",
  "productName": "Photo Album",
  "publishName": "Photo Album",
  "version": "2.9.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build_with_ep": "electron-packager . Album --overwrite --asar --platform=win32 --arch=x64 --prune=true --app-version=2.9 --out=release-builds  --icon=icon1.ico",
    "build": "build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.5.0",
    "electron-winstaller": "^2.7.0"
  },
  "dependencies": {
    "asar": "^0.14.6",
    "update-electron-app": "^1.3.0"
  },
  "build": {
    "icon": "icon2.ico"
  }
}
